#!/bin/bash

set -o xtrace
set -o errexit

# Ensure tests pass
cargo test

# Ensure all code is correctly formatted
cargo fmt --all -- --check

# Run Clippy with lints as configured in Cargo.toml
cargo clippy
