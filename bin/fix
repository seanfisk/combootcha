#!/bin/bash

set -o errexit
set -o xtrace

# Apply spelling and lint suggestions before auto-formatting as these could change the line length, etc.
typos --write-changes
cargo clippy --fix \
      --allow-dirty --allow-staged # Ignore version control state entirely

cargo fmt --all
